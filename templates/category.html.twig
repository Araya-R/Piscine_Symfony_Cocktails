{% extends "base.html.twig" %}

 {% block metatitle %}
 Les category
 {% endblock metatitle %}
{% block main %}

	<main>
		<h3>Categories:</h3>
		<div>
            {% for category in category %}
			<h4>
			{{ category.nom}}
			</h4>
			<p>{{ category.description }}</p>
            {% endfor %}
		</div>
	</main>

{% endblock main %} 
