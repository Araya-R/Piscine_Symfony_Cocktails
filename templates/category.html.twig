{% extends "base.html.twig" %}

 {% block metatitle %}
 Les category
 {% endblock metatitle %}
{% block main %}

	<main>
		<h3>Categories:</h3>
		<div>
            {% for category in category %}
			<h4>
			<a href="{{ path('show-category', { 'id': category.id }) }}">{{ category.nom}}</a>
			</h4>
            {% endfor %}
		</div>
	</main>

{% endblock main %} 
