{% include "partials/_header.html.twig" %}

<main class="container">
	<div class="cocktail-card">
		<h1>{{ cocktail.nom }}</h1>
		<img src="{{ cocktail.image }}" width="50%">
		<p>{{ cocktail.date_creation }}</p>
		<p>{{ cocktail.description }}</p>

		<h3>Ingrédients :</h3>
		<ul>
			{% for ingredient in cocktail.ingredients %}
				<li>{{ ingredient }}</li>
			{% endfor %}
		</ul>

	</div>
</main>
